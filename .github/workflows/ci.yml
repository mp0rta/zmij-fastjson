name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build-install-test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.10", "3.11", "3.12", "3.13", "3.14"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          allow-prereleases: true

      - name: Upgrade build tooling
        run: python -m pip install -U pip setuptools wheel build

      - name: Build artifacts (sdist + wheel)
        run: |
          python -m build pyzmij -s -w
          python -m build fastjson -s -w

      - name: Create clean venv (Unix)
        if: runner.os != 'Windows'
        run: |
          python -m venv .venv-test
          .venv-test/bin/python -m pip install -U pip pytest

      - name: Create clean venv (Windows)
        if: runner.os == 'Windows'
        run: |
          python -m venv .venv-test
          .venv-test\Scripts\python -m pip install -U pip pytest

      - name: Install wheels into clean venv (Unix)
        if: runner.os != 'Windows'
        run: |
          .venv-test/bin/python -m pip install pyzmij/dist/*.whl
          .venv-test/bin/python -m pip install fastjson/dist/*.whl

      - name: Install wheels into clean venv (Windows)
        if: runner.os == 'Windows'
        run: |
          $pyzmijWheels = @(Get-ChildItem -Path pyzmij/dist -Filter "*.whl")
          if ($pyzmijWheels.Count -ne 1) { throw "Expected exactly 1 pyzmij wheel, found $($pyzmijWheels.Count)" }
          .venv-test\Scripts\python -m pip install $pyzmijWheels[0].FullName

          $fastjsonWheels = @(Get-ChildItem -Path fastjson/dist -Filter "*.whl")
          if ($fastjsonWheels.Count -ne 1) { throw "Expected exactly 1 fastjson wheel, found $($fastjsonWheels.Count)" }
          .venv-test\Scripts\python -m pip install $fastjsonWheels[0].FullName

      - name: Smoke import (Unix)
        if: runner.os != 'Windows'
        run: .venv-test/bin/python -c "import pyzmij, fastjson; print('ok')"

      - name: Smoke import (Windows)
        if: runner.os == 'Windows'
        run: .venv-test\Scripts\python -c "import pyzmij, fastjson; print('ok')"

      - name: Run tests from a temp working directory (Unix)
        if: runner.os != 'Windows'
        run: |
          tmpdir="$(mktemp -d)"
          cd "$tmpdir"
          "$GITHUB_WORKSPACE/.venv-test/bin/python" -m pytest -q --import-mode=importlib "$GITHUB_WORKSPACE/pyzmij/tests/" --ignore="$GITHUB_WORKSPACE/pyzmij/tests/test_repr_compat.py"
          "$GITHUB_WORKSPACE/.venv-test/bin/python" -m pytest -q --import-mode=importlib "$GITHUB_WORKSPACE/fastjson/tests/"

      - name: Run tests from a temp working directory (Windows)
        if: runner.os == 'Windows'
        run: |
          $tmp = New-Item -ItemType Directory -Force -Path (Join-Path $env:RUNNER_TEMP "pytest-tmp")
          Set-Location $tmp.FullName
          & "$env:GITHUB_WORKSPACE\\.venv-test\\Scripts\\python" -m pytest -q --import-mode=importlib "$env:GITHUB_WORKSPACE\\pyzmij\\tests\\" --ignore="$env:GITHUB_WORKSPACE\\pyzmij\\tests\\test_repr_compat.py"
          & "$env:GITHUB_WORKSPACE\\.venv-test\\Scripts\\python" -m pytest -q --import-mode=importlib "$env:GITHUB_WORKSPACE\\fastjson\\tests\\"

  sdist-install-test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
        python-version: ["3.14"]

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
          allow-prereleases: true

      - name: Upgrade build tooling
        run: python -m pip install -U pip setuptools wheel build

      - name: Build sdist
        run: |
          python -m build pyzmij -s
          python -m build fastjson -s

      - name: Create clean venv (Unix)
        if: runner.os != 'Windows'
        run: |
          python -m venv .venv-sdist
          .venv-sdist/bin/python -m pip install -U pip pytest

      - name: Create clean venv (Windows)
        if: runner.os == 'Windows'
        run: |
          python -m venv .venv-sdist
          .venv-sdist\Scripts\python -m pip install -U pip pytest

      - name: Install from sdist in clean venv (Unix)
        if: runner.os != 'Windows'
        run: |
          .venv-sdist/bin/python -m pip install pyzmij/dist/*.tar.gz
          .venv-sdist/bin/python -m pip install fastjson/dist/*.tar.gz

      - name: Install from sdist in clean venv (Windows)
        if: runner.os == 'Windows'
        run: |
          $pyzmijSdists = @(Get-ChildItem -Path pyzmij/dist -Filter "*.tar.gz")
          if ($pyzmijSdists.Count -ne 1) { throw "Expected exactly 1 pyzmij sdist, found $($pyzmijSdists.Count)" }
          .venv-sdist\Scripts\python -m pip install $pyzmijSdists[0].FullName

          $fastjsonSdists = @(Get-ChildItem -Path fastjson/dist -Filter "*.tar.gz")
          if ($fastjsonSdists.Count -ne 1) { throw "Expected exactly 1 fastjson sdist, found $($fastjsonSdists.Count)" }
          .venv-sdist\Scripts\python -m pip install $fastjsonSdists[0].FullName

      - name: Smoke import (Unix)
        if: runner.os != 'Windows'
        run: .venv-sdist/bin/python -c "import pyzmij, fastjson; print('ok')"

      - name: Smoke import (Windows)
        if: runner.os == 'Windows'
        run: .venv-sdist\Scripts\python -c "import pyzmij, fastjson; print('ok')"

      - name: Run tests from a temp working directory (Unix)
        if: runner.os != 'Windows'
        run: |
          tmpdir="$(mktemp -d)"
          cd "$tmpdir"
          "$GITHUB_WORKSPACE/.venv-sdist/bin/python" -m pytest -q --import-mode=importlib "$GITHUB_WORKSPACE/pyzmij/tests/" --ignore="$GITHUB_WORKSPACE/pyzmij/tests/test_repr_compat.py"
          "$GITHUB_WORKSPACE/.venv-sdist/bin/python" -m pytest -q --import-mode=importlib "$GITHUB_WORKSPACE/fastjson/tests/"

      - name: Run tests from a temp working directory (Windows)
        if: runner.os == 'Windows'
        run: |
          $tmp = New-Item -ItemType Directory -Force -Path (Join-Path $env:RUNNER_TEMP "pytest-tmp-sdist")
          Set-Location $tmp.FullName
          & "$env:GITHUB_WORKSPACE\\.venv-sdist\\Scripts\\python" -m pytest -q --import-mode=importlib "$env:GITHUB_WORKSPACE\\pyzmij\\tests\\" --ignore="$env:GITHUB_WORKSPACE\\pyzmij\\tests\\test_repr_compat.py"
          & "$env:GITHUB_WORKSPACE\\.venv-sdist\\Scripts\\python" -m pytest -q --import-mode=importlib "$env:GITHUB_WORKSPACE\\fastjson\\tests\\"
